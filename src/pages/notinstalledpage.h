#pragma once

#include <QWidget>
#include <QLabel>
#include <QPushButton>

class NotInstalledPage : public QWidget
{
    Q_OBJECT
public:
    explicit NotInstalledPage(QWidget *parent = nullptr);
};

