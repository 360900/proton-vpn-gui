#pragma once
// Transitional forwarder - the implementation moved to core/hostCommand.h
// as part of the vpncore extraction. Will be removed once all includes are
// updated to the new path.
#include "../core/hostCommand.h"
